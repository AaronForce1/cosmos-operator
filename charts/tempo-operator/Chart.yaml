apiVersion: v2
name: tempo-operator
description: >-
  Kubernetes operator for Tempo (reth + Commonware) full nodes and validators.
  Installs the TempoFullNode CRD and the operator deployment.
type: application
home: https://github.com/aaronforce1/cosmos-operator
sources:
  - https://github.com/aaronforce1/cosmos-operator
# Chart version. Bump on any chart change.
version: 0.1.0
# Operator image tag deployed by default (see values.image.tag to override).
appVersion: "latest"
keywords:
  - tempo
  - reth
  - blockchain
  - operator
